# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read [`.github/copilot-instructions.md`](.github/copilot-instructions.md) first** — it is the single source of truth for this repo: what it is (GNU Stow dotfiles at `~/projects/conf`), how to deploy it (`make` / `make delete`), the package → `$HOME` mapping, unstowed root files, gitignored machine-local files, submodules, and the Neovim/zsh/tmux/theming conventions.

Keep that file up to date instead of duplicating it here; add Claude-specific notes below only when they don't belong in the shared instructions.
